{
  "name": "calendario",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20.0.0 <21.0.0",
    "yarn": "^1.22.0"
  },
  "scripts": {
    "start": "cd frontend && yarn install && yarn build && yarn preview --port 3000 --host",
    "dev": "concurrently \"cd backend && yarn dev\" \"cd frontend && yarn dev\"",
    "build": "cd frontend && yarn install && yarn build",
    "install:all": "yarn install && cd frontend && yarn install && cd ../backend && yarn install",
    "start:backend": "cd backend && yarn start",
    "start:frontend": "cd frontend && yarn start"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "firebase": "^11.10.0",
    "lucide-react": "^0.525.0",
    "react-router-dom": "^7.6.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11",
    "concurrently": "^8.2.2"
  }
}
